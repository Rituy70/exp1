// Add a simple JS to handle form interactions, alert messages, etc.
document.querySelector('form').addEventListener('submit', function (e) {
    e.preventDefault();
    alert('Form Submitted Successfully!');
  });
  